You are building the Day 1 Jupyter notebooks for an applied data-science lecture series.

Read these files FIRST for full context:
- CLAUDE.md  (project conventions + the "Day 1 notebooks - build spec")
- docs/day1_neural_networks_slides.md  (slide-by-slide content the notebooks pair with)
- docs/day1_presenter_guide.md  (the math, especially the backprop walkthrough with worked numbers)

Build exactly TWO notebooks in notebooks/, following the STRICT notebook code
style in CLAUDE.md: camelCase functions, snake_case variables, lowercase and
sparing comments, short docstrings, NO emojis, NO separator characters (no ====
or ----) in print output, and no printed indentation. Write all generated
figures/models to notebooks/outputs/. Use markdown cells to mark stages and note
the matching slide. Each notebook must run top-to-bottom with no errors, and both
must be SELF-CONTAINED (no dependency on each other).

1. notebooks/01_mlp_from_scratch.ipynb
   A 2-2-1 MLP from scratch in NumPy with sigmoid activations. Define the XOR
   data in the notebook. Implement the forward pass, a loss, a MANUAL backward
   pass (mirror the chain-rule / delta-caching structure in
   docs/day1_presenter_guide.md), and a gradient-descent training loop. Train it
   to solve XOR. Plot the loss curve and the learned non-linear decision boundary.

2. notebooks/02_pytorch_mlp_fashionmnist.ipynb
   The same MLP rebuilt with PyTorch nn.Module. Load Fashion-MNIST via
   torchvision, use CrossEntropyLoss + Adam, and the canonical 5-line training
   loop (forward, loss, zero_grad, backward, step). Train a few epochs, plot loss
   and accuracy, and save a sample figure to notebooks/outputs/.

After writing each notebook, EXECUTE it end-to-end with:
  jupyter nbconvert --to notebook --execute --inplace <notebook>
and fix any errors so it runs cleanly. Install any missing packages as needed
(numpy, matplotlib, torch, torchvision, jupyter). Do not modify files outside
notebooks/ and notebooks/outputs/ unless necessary.

When finished, print a short summary of what each notebook contains and confirm
that both executed without errors.
